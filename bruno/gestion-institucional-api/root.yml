info:
  name: root
  type: http
  seq: 2

http:
  method: GET
  url: "{{API}}/institution/tree-directory"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
