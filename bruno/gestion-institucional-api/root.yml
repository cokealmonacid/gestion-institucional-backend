info:
  name: root
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/institution/tree-directory"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
