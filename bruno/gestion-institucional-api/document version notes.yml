info:
  name: document version notes
  type: http
  seq: 25

http:
  method: GET
  url: "{{API}}/documents/{{document_id}}/versions/notes"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
