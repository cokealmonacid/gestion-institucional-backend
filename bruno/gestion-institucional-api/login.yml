info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: "{{API}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "testapi@example.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
