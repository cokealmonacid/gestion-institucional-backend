info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: "{{API}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "ivoapi@test.cl",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
