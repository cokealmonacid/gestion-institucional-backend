info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: "{{API}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "user@example.test",
        "password": "replace-with-local-password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
