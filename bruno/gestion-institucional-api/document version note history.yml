info:
  name: document version note history
  type: http
  seq: 26

http:
  method: GET
  url: "{{API}}/documents/{{document_id}}/versions/{{version_id}}/note/history"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
