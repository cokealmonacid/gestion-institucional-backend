info:
  name: profile
  type: http
  seq: 11

http:
  method: GET
  url: "{{API}}/user/profile"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
