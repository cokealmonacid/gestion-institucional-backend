info:
  name: user profile
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/user/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
