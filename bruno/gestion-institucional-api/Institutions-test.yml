info:
  name: Institutions-test
  type: http
  seq: 1

http:
  method: GET
  url: "{{API}}/institutions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
