info:
  name: Show tags
  type: http
  seq: 42

http:
  method: GET
  url: "{{API}}/institution/tag"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
