info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{API}}/auth/logout"
  headers:
    - name: Accept
      value: application/json
      disabled: true
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{API}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
