info:
  name: create node
  type: http
  seq: 9

http:
  method: POST
  url: "{{API}}/institution/tree-directory"
  body:
    type: json
    data: |-
      {
        "name": "Contratos",
        "parent_id": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
