info:
  name: document version comment history
  type: http
  seq: 26

http:
  method: GET
  url: "{{API}}/documents/{{document_id}}/versions/{{version_id}}/comments"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
