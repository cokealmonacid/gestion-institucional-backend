info:
  name: Show users from institution
  type: http
  seq: 39

http:
  method: GET
  url: "{{API}}/institution/users"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{TOKEN}}"

runtime:
  variables:
    - name: INSTITUTION_ID
      value: 00000000-0000-0000-0000-000000000001

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
