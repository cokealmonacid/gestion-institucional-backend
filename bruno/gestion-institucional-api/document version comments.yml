info:
  name: document version comments
  type: http
  seq: 25

http:
  method: GET
  url: "{{API}}/documents/{{document_id}}/versions/comments"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
