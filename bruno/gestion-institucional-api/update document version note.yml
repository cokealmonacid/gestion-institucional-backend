info:
  name: update document version note
  type: http
  seq: 27

http:
  method: PATCH
  url: "{{API}}/documents/{{document_id}}/versions/{{version_id}}/note"
  body:
    type: json
    data:
      note: Nota institucional actualizada desde Bruno
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
